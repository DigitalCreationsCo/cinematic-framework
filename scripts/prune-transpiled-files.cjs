const fs = require('fs'); const clean = (d) => { fs.readdirSync(d).forEach(f => { const p = require('path').join(d, f); if (fs.lstatSync(p).isDirectory() && !(["node_modules", "build", "dist"]).includes(f)) clean(p); else if ([ '.ts', '.js', '.map' ].some(e => f.endsWith(e)) && !f.endsWith('.ts') || f.endsWith('.d.ts')) { fs.unlinkSync(p); console.log('Deleted:', p); } }); }; clean('./website');